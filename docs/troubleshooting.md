# Troubleshooting

`UNKNOWN` means a required constraint was not evaluated. Check IAM, API throttling, network reachability, stale snapshots, unsupported resource availability, and quota statement parsing.
